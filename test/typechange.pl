#!/usr/bin/perl -w
use strict qw(vars subs);
use warnings;

use Data::Dumper qw(Dumper);

my %firstHash = (
	module1 => {
		aString => '1',
		aNumber => 1,
	},
	module2 => {
		aString => '2',
		aNumber => 2,
	},
);


